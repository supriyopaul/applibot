vector-store:
  lancedb:
    resume-store:
      path: "data/sample-lancedb"
      table-name: "resume"
      schema: ResumeSchema

    info-store:
      path: "data/sample-lancedb"
      table-name: "info"
      schema: InfoSchema

store:
  postgres:
    user-store:
      host: localhost
      port: 5432
      database: applibot_db
      user: applibot_user
      password: change_this_password
      table-name: users
      schema: UserSchema

chat-model:
  model-name: "gpt-4"
  temperature: 0.0
  key: xxx
  cache: False

embeddings-model:
  name: "OpenAIEmbeddings"
  key: xxx
  
service:
  host: 0.0.0.0
  port: 9000
  workers: 5